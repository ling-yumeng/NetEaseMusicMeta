# neteasemusicmeta

To install dependencies:

```bash
bun install
```

To build:

```
mkdir build
cmake ../Cpp && cmake --build .
```

To run:

```bash
./nemeta_gui
```

This project was created using `bun init` in bun v1.2.16. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime.
